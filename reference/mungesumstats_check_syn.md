# Check colname synonyms

Check whether a table has headers that can be mapped to an attribute in
MungeSumstats

## Usage

``` r
mungesumstats_check_syn(dat, col_name = "P-value", col_type = "P")
```
