# `sumstatsColHeaders` from MungeSumstats

`sumstatsColHeaders` from MungeSumstats

## Usage

``` r
data("sumstatsColHeaders")
```

## Format

An object of class `data.frame` with 205 rows and 2 columns.

## Examples

``` r
if (FALSE) { # \dontrun{
sumstatsColHeaders <- MungeSumstats:::sumstatsColHeaders
usethis::use_data(sumstatsColHeaders, overwrite=TRUE)
} # }
```
