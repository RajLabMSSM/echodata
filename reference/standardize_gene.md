# Standardize genes

Convert ensembl IDs to gene symbols.

## Usage

``` r
standardize_gene(dat, Gene = "gene", verbose = TRUE)
```

## Arguments

- dat:

  GWAS/QTL summary statistics data.

- Gene:

  Name of the gene column in `dat`.

- verbose:

  Print messages.
