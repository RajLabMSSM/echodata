# Is an object of class GRanges

Check whether an object is of the class
[GRanges](https://rdrr.io/pkg/GenomicRanges/man/GRanges-class.html).

## Usage

``` r
is_granges(obj)
```

## Arguments

- obj:

  Any R object.

## Value

Boolean.

## Examples

``` r
bool <- echodata::is_granges(mtcars)
```
